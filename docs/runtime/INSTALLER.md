# Installer

The installer makes the Hermes Agent Continuity Kit installable from the public repository root. It supports `--profile minimal|standard|advanced`, `--target`, `--dry-run`, `--non-interactive`, and `--config`. Default installation creates placeholder config only and does not enable schedulers, create production archives, advance cursors, decrypt archives, or read private message content.
