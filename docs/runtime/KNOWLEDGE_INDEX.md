# Knowledge Index

The Knowledge Index is an optional advanced-profile index over approved safe summaries. The MVP uses a deterministic local summary file by default; SQLite FTS can be enabled later as an optional local optimization.
