# Operations

Operational defaults are safe: memory audit is report-only, archive is dry-run, and recovery refresh creates a safe handoff without decrypting archives. Archive production, cursor advancement, scheduler enablement, and decrypt operations require explicit approval outside the default runtime MVP.
