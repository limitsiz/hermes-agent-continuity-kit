# Configuration

Use `config/example.hermes-continuity.yaml` or one of the install examples as a placeholder-only starting point. Put installation-specific values only in a private target runtime directory, not in the public repository.
