# Scheduler Templates

Cron and systemd files in `scheduler/` are examples only. The installer does not install or enable production timers. Operators must review, copy, and enable any scheduler entry through a separate approval gate.
