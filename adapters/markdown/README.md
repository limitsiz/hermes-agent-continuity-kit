# Markdown Knowledge Workspace Adapter

The default Knowledge Workspace Adapter writes safe summaries to a Markdown folder. It is product-agnostic and does not sync raw transcripts, decrypted archives, or secrets.
